# =============================================================================
# Face Recognition Attendance System - Frontend Environment Configuration
# =============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to '.env' in the same directory (frontend/.env)
# 2. Update the values below with your specific configuration
# 3. For production, update API URLs to your domain
# =============================================================================

# API Configuration
REACT_APP_API_URL=http://localhost:8000          # Backend API URL
REACT_APP_WS_URL=ws://localhost:8000            # WebSocket URL for real-time updates

# Application Settings
REACT_APP_NAME=Face Recognition Attendance System
REACT_APP_VERSION=1.0.0

# Development Settings
GENERATE_SOURCEMAP=true                          # Set to false for production
FAST_REFRESH=true                               # React Fast Refresh for development

# Production Settings (uncomment and modify for production)
# GENERATE_SOURCEMAP=false
# REACT_APP_API_URL=https://yourdomain.com/api
# REACT_APP_WS_URL=wss://yourdomain.com/ws
